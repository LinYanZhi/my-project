伪代码:

接收参数
if 第一个参数 in ['a', 'add', 'activate']
    判断当前是否已激活
    if 已激活：
        提示并退出
    未激活
    将当前所有环境另存为文件，文件路径是当前文件所在目录下，文件名是%WT_SESSION%.bat
    读取配置文件中的环境变量和环境路径
    将用户配置的环境和之前的环境相互合并
    设置标志：已激活
    提示并结束

if 第二个参数 in ['d', 'del', 'deactivate']
    判断当前是否已激活
    if 未激活：
        提示并退出
    已激活
    读取之前另存的文件
    恢复到之前的环境

if 第二个参数 in ['l', 'list']:
    读取当前路径下的envs目录下的目录，然后列出来
    如果当前已激活环境在在列出的环境名前面加上 * 